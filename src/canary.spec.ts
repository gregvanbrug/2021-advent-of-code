import { expect } from 'chai';

describe('Canary Test', () => {
  it('true should be true', () => {
    const expected = true;
    const actual = true;
    expect(expected).to.equal(actual);
  });
});
